local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    local Window = Library.CreateLib("NOTLONTOP BETA", "Synapse")
local Tab = Window:NewTab("player")
local Section = Tab:NewSection("main player")
Section:NewSlider("ws", "changes players walkspeed", 500, 16, function(s) -- 500 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)
Section:NewButton("fly e", "u know?", function()
print("clicked")  repeat wait() 
	until game.Players.LocalPlayer and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:findFirstChild("Head") and game.Players.LocalPlayer.Character:findFirstChild("Humanoid") 
local mouse = game.Players.LocalPlayer:GetMouse() 
repeat wait() until mouse
local plr = game.Players.LocalPlayer 
local torso = plr.Character.Head 
local flying = false
local deb = true 
local ctrl = {f = 0, b = 0, l = 0, r = 0} 
local lastctrl = {f = 0, b = 0, l = 0, r = 0} 
local maxspeed = 50 
local speed = 0 

function Fly() 
local bg = Instance.new("BodyGyro", torso) 
bg.P = 9e4 
bg.maxTorque = Vector3.new(9e9, 9e9, 9e9) 
bg.cframe = torso.CFrame 
local bv = Instance.new("BodyVelocity", torso) 
bv.velocity = Vector3.new(0,0.1,0) 
bv.maxForce = Vector3.new(9e9, 9e9, 9e9) 
repeat wait() 
plr.Character.Humanoid.PlatformStand = true 
if ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0 then 
speed = speed+.5+(speed/maxspeed) 
if speed > maxspeed then 
speed = maxspeed 
end 
elseif not (ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0) and speed ~= 0 then 
speed = speed-1 
if speed < 0 then 
speed = 0 
end 
end 
if (ctrl.l + ctrl.r) ~= 0 or (ctrl.f + ctrl.b) ~= 0 then 
bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (ctrl.f+ctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(ctrl.l+ctrl.r,(ctrl.f+ctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed 
lastctrl = {f = ctrl.f, b = ctrl.b, l = ctrl.l, r = ctrl.r} 
elseif (ctrl.l + ctrl.r) == 0 and (ctrl.f + ctrl.b) == 0 and speed ~= 0 then 
bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (lastctrl.f+lastctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(lastctrl.l+lastctrl.r,(lastctrl.f+lastctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed 
else 
bv.velocity = Vector3.new(0,0.1,0) 
end 
bg.cframe = game.Workspace.CurrentCamera.CoordinateFrame * CFrame.Angles(-math.rad((ctrl.f+ctrl.b)*50*speed/maxspeed),0,0) 
until not flying 
ctrl = {f = 0, b = 0, l = 0, r = 0} 
lastctrl = {f = 0, b = 0, l = 0, r = 0} 
speed = 0 
bg:Destroy() 
bv:Destroy() 
plr.Character.Humanoid.PlatformStand = false 
end 
mouse.KeyDown:connect(function(key) 
if key:lower() == "e" then 
if flying then flying = false 
else 
flying = true 
Fly() 
end 
elseif key:lower() == "w" then 
ctrl.f = 1 
elseif key:lower() == "s" then 
ctrl.b = -1 
elseif key:lower() == "a" then 
ctrl.l = -1 
elseif key:lower() == "d" then 
ctrl.r = 1 
end 
end) 
mouse.KeyUp:connect(function(key) 
if key:lower() == "w" then 
ctrl.f = 0 
elseif key:lower() == "s" then 
ctrl.b = 0 
elseif key:lower() == "a" then 
ctrl.l = 0 
elseif key:lower() == "d" then 
ctrl.r = 0 
end 
end)
Fly()
end)
Section:NewKeybind("tp tool must read the dots", "press K to get a tp tool", Enum.KeyCode.K, function()
	print("You just clicked the bind") mouse = game.Players.LocalPlayer:GetMouse()
tool = Instance.new("Tool")
tool.RequiresHandle = false
tool.Name = "Click Teleport"
tool.Activated:connect(function()
local pos = mouse.Hit+Vector3.new(0,2.5,0)
pos = CFrame.new(pos.X,pos.Y,pos.Z)
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = pos
end)
tool.Parent = game.Players.LocalPlayer.Backpack
end)
Section:NewButton("Universal Lock (any da hood", "Lock Mainly For da hood", function()
    print("Clicked") getgenv().AimPart = "HumanoidRootPart" -- Con esto eliges donde apuntar: {UpperTorso, LowerTorso, HumanoidRootPart, Head}
getgenv().AimlockToggleKey = "Y" -- Tecla para apagar y prender el lock
getgenv().AimRadius = 50 -- Distancia de la persona a la que lockearas
getgenv().TeamCheck = false -- Dejar esto desactivado
getgenv().PredictMovement = true -- Esto es para que el lock apunte con predicción, dejar activado
getgenv().PredictionVelocity = 7 -- Esto es con cuanta predicción quieres que apunte, aquí puedes ir probando depende de el ping que tengas OJO entre mas grande sea el numero menor prediccion tendra

loadstring(game:HttpGet("https://arevalacarchives.000webhostapp.com/FuckHoo", true))()
end)


local Tab = Window:NewTab("Best Hubs")
local Section = Tab:NewSection("Hubs")
Section:NewButton("DomainHub", "Hub", function()
    print("Clicked") loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexsoftworks/Domain/main/source'),true))()
end)

Section:NewButton("EzHub", "Hub With 150 Games+", function()
    print("Clicked") loadstring(game:HttpGet(('https://raw.githubusercontent.com/debug420/Ez-Industries-Launcher-Data/master/Launcher.lua'),true))()
end)
Section:NewButton("UTG Hub", "Hub", function()
    print("Clicked") loadstring(game:HttpGet("https://pastebin.com/raw/fwQNynHg"))()
end)
Section:NewButton("Selexity Hub", "Best Reanimation Hubs", function()
    print("Clicked") loadstring(game:HttpGet("https://raw.githubusercontent.com/stangithuboffical/selexity/main/Main", true))()
end)
Section:NewButton("BruhHub","Trolling for Ragdoll Engine" , function()
    print("Clicked") loadstring(game:HttpGet("https://bruh.keshhub.com/.lua"))()
end)
Section:NewButton("SnowHub", "Hub (trolling)", function()
    print("Clicked") loadstring(game:HttpGet("https://snowhub.dev/script.lua", true))()
end)
local Tab = Window:NewTab("READ ME")
local Section = Tab:NewSection("CREDITS iateforever on rbx")
Section:NewButton("V0.7", "VERSION 0.7", function()
    print("Clicked")
end)
Section:NewButton("UPDATING WEEKLY", "updating", function()
    print("Clicked")
end)
Section:NewButton("dont forget its beta", "BETA", function()
    print("Clicked")
end)




